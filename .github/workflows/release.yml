name: release
on:
  push:
    branches:
      - 'master'

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      matrix:
        os: [
          ubuntu-24.04,
          ubuntu-24.04-arm
        ]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Login DockerHub
        uses: docker/login-action@v4
        with:
          registry: docker.io
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        run: |
          docker build \
          -t ghcr.io/stardogg/docker-php-5.2:${{github.run_number}}-$(uname -m) \
          -t docker.io/stardogg/docker-php-5.2:${{github.run_number}}-$(uname -m) \
          -f Dockerfile .

      - name: Push GHCR
        run: |
          docker push ghcr.io/stardogg/docker-php-5.2 --all-tags

      - name: Push DockerHub
        run: |
          docker push docker.io/stardogg/docker-php-5.2 --all-tags

  push-manifest:
    needs: build
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        registry: [
          docker.io,
          ghcr.io
        ]
        include:
          - registry: docker.io
            image: docker.io/stardogg/docker-php-5.2
          - registry: ghcr.io
            image: ghcr.io/stardogg/docker-php-5.2

    steps:
      - name: Pull
        run: |
          docker pull ${{ matrix.image }}:${{github.run_number}}-x86_64
          docker pull ${{ matrix.image }}:${{github.run_number}}-aarch64

      - name: Login DockerHub
        uses: docker/login-action@v4
        with:
          registry: docker.io
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Manifest
        run: |
          docker manifest create ${{ matrix.image }}:${{github.run_number}} \
            ${{ matrix.image }}:${{github.run_number}}-x86_64 \
            ${{ matrix.image }}:${{github.run_number}}-aarch64
          docker manifest create ${{ matrix.image }}:latest \
            ${{ matrix.image }}:${{github.run_number}}-x86_64 \
            ${{ matrix.image }}:${{github.run_number}}-aarch64

      - name: Push
        run: |
          docker manifest push --purge ${{ matrix.image }}:${{github.run_number}}
          docker manifest push --purge ${{ matrix.image }}:latest
